# Epic 2: CLI Query Interface

Agent-friendly CLI querying interface.

## Tasks

- [x] Positional ES|QL query argument via `esqlorer query "<query>"`
- [x] `-c, --context` flag for server context
- [x] `--from` flag for lower time bound
- [x] `--to` flag for upper time bound
- [x] `-o, --output` flag for output format
- [x] `discoverable -h/--help`

## Status: ✅ COMPLETED
